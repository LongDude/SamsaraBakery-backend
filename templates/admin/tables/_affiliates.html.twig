<div id="twigHTMLContainer">
<table id="dynamic-table"></table>
<button id="add-item-btn" class="btn btn-success mb-2">Добавить товар</button>

<style>
 /* Стили для выпадающего autocomplete-списка */
.ui-autocomplete {
  z-index: 2000 !important;
  background: #fff;
  border-radius: 0.375rem;
  border: 1px solid #ced4da;
  box-shadow: 0 0.5rem 1rem rgba(0,0,0,.15);
  padding: 0;
  list-style: none;
  max-height: 250px;
  overflow-y: auto;
  font-size: 1rem;
}

.ui-menu-item {
  margin: 0;
}

.ui-menu-item-wrapper {
  padding: 0.5rem 1rem;
  cursor: pointer;
  transition: background 0.2s, color 0.2s;
  border-bottom: 1px solid #f1f1f1;
}

.ui-menu-item-wrapper:last-child {
  border-bottom: none;
}

.ui-state-active, .ui-menu-item-wrapper.ui-state-active {
  background: #0d6efd;
  color: #fff;
  border-radius: 0.375rem;
}

.ui-helper-hidden-accessible {
  display: none;
}
</style>


<!-- Модальное окно для добавления/редактирования -->
<div class="modal fade" id="itemModal" tabindex="-1" aria-labelledby="itemModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <form id="item-form">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="itemModalLabel">Филиал</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Закрыть"></button>
        </div>
        <div class="modal-body">
          <input type="hidden" id="item-id">
          <div class="mb-3">
            <label for="item-address" class="form-label">Адрес</label>
            <input type="text" class="form-control" id="item-address" required>
          </div>
          <div class="mb-3">
            <label for="item-contact_number" class="form-label">Контактный номер</label>
            <input type="number" class="form-control" id="item-contact_number" step="0.01" required>
          </div>
          <div class="mb-3">
            <label for="item-manager" class="form-label">Менеджер</label>
            <input type="text" class="form-control" id="item-manager" step="0.01" required>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Отмена</button>
          <button type="submit" class="btn btn-primary" id="save-item-btn">Сохранить</button>
        </div>
      </div>
    </form>
  </div>
</div>
</div>

<script type="text/javascript" src="{{ path('admin_table_affiliates_js') }}"></script>