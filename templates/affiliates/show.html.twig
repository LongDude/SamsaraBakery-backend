{% extends 'base.html.twig' %}

{% block title %}Affiliates{% endblock %}

{% block body %}
    <h1>Affiliates</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affiliate.id }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ affiliate.address }}</td>
            </tr>
            <tr>
                <th>Contact_number</th>
                <td>{{ affiliate.contactNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_affiliates_index') }}">back to list</a>

    <a href="{{ path('app_affiliates_edit', {'id': affiliate.id}) }}">edit</a>

    {{ include('affiliates/_delete_form.html.twig') }}
{% endblock %}
