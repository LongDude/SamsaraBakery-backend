<form method="post" action="{{ path('app_production_plan_delete', {'id': production_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production_plan.id) }}">
    <button class="btn">Delete</button>
</form>
