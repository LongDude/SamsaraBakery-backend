{% extends 'base.html.twig' %}

{% block title %}ProductionPlan{% endblock %}

{% block body %}
    <h1>ProductionPlan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ production_plan.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ production_plan.date ? production_plan.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ production_plan.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_production_plan_index') }}">back to list</a>

    <a href="{{ path('app_production_plan_edit', {'id': production_plan.id}) }}">edit</a>

    {{ include('production_plan/_delete_form.html.twig') }}
{% endblock %}
